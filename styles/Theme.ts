import { createTheme } from '@mui/material/styles'

const Theme = createTheme({
  palette: {
    primary: {
      main: '#eb9f9f',
      contrastText: '#fff',
    },
    secondary: {
      main: '#f8ecc9',
    },
  },
})

export default Theme
